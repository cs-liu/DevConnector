# 《代码整洁之道》读书分享：让代码不言自明 (第4-6章)

分享人：刘昌盛

[TOC]

大家好。在过去的一周里，我们通过前三章学习了如何给变量和函数起一个“名副其实”的名字，以及如何将函数重构得“短小精悍且只做一件事”。如果说前三章教给我们的是如何雕琢微观的“代码砖块”，那么今天我要分享的第 4、5、6 章，则是教我们如何将这些砖块砌成一面**结构清晰、整齐划一、且易于长期维护的墙**。

今天，我们将直击日常开发中最容易引发争议、也最容易被忽视的三个领域：

1. **注释 (Comments)：** 为什么说注释往往是代码设计失败的标志？
2. **格式 (Formatting)：** 为什么代码的排版不是个人审美的私事，而是团队协作的最高纪律？
3. **对象与数据结构 (Objects and Data Structures)：** 为什么我们写了多年的 Class，却依然写出的是面向过程的代码？

------

## 第一部分：第四章 注释 (Comments) —— 别给糟糕的代码加注释，重写吧！

“别给糟糕的代码加注释——重新写吧。”（Don't comment bad code—rewrite it.）这是 Brian W. Kernighan 和 P. J. Plauger 的名言，也是整个第四章的灵魂。

### 1. 注释的本质：一种“必要的恶”

很多初学者甚至部分资深开发者都有一个误区：代码里注释越多，说明我越负责，代码越好。Bob 大叔（作者）严厉地驳斥了这一观点：**注释的恰当用法，是弥补我们在用代码表达意图时遭遇的失败。**

为什么说注释是一种失败？

- **注释会撒谎：** 代码在不断演进，功能在修改，变量在重命名，但几乎没有程序员会在修改代码时，严格同步更新所有的注释。久而久之，注释和代码脱节，变成了误导后人的“地雷”。
- **代码应该自解释：** 如果你需要用人类语言来解释机器语言，说明你的机器语言写得不够清晰。

**实战对比：**

```typescript
// 糟糕：用注释来解释复杂的条件判断
// 检查用户是否符合试用期且未购买过专业版，并且当前不在冻结状态
if ((user.registerDays < 30) && !user.hasProPlan && user.status !== 2) {
    showUpgradeBanner();
}

// 整洁：用自解释的函数名（或计算属性）替代注释
if (user.isEligibleForProTrial()) {
    showUpgradeBanner();
}
```

在上述整洁的代码中，我们根本不需要注释。阅读者一秒钟就能看懂业务意图。如果他想知道具体的判断细节，点进 `isEligibleForProTrial()` 函数查看即可。

### 2. 什么是“好注释”？(The Good Comments)

当然，我们不能走向极端。在某些特定场景下，注释不仅是必要的，甚至是必需的。书中列举了以下几种合法的“好注释”：

- **1) 法律信息 (Legal Comments)：** 版权声明、开源 License 声明。这通常放在每个文件的头部。

- **2) 提供信息的注释 (Informative Comments)：** 有时，代码本身无法完全传达某些特殊格式或模式。

  ```typescript
  // 匹配格式如：kk:mm:ss EEE, MMM dd, yyyy
  const timeRegex = /\d{2}:\d{2}:\d{2} \w{3}, \w{3} \d{2}, \d{4}/;
  ```

- **3) 对意图的解释 (Explanation of Intent)：** 这是非常重要的一种注释。它不解释代码**做了什么**，而是解释代码**为什么**要这么做。这通常涉及业务上的特殊决策或妥协。

  ```typescript
  // 这里故意使用了同步的阻塞调用而不是异步，
  // 因为我们需要确保在渲染任何 Vue 组件之前，基础的全局配置已经完全就绪，否则会导致组件状态丢失。
  fetchGlobalConfigSync(); 
  ```

- **4) 阐释 (Clarification)：** 当你调用第三方库、或者无法修改的旧代码时，其参数或返回值极其晦涩，你需要用注释把它们翻译成可读的语言。

- **5) 警告某种后果 (Warning of Consequences)：** 警告其他开发者不要随意触碰或修改某段代码。

  ```typescript
  // 警告：不要在此处使用 Promise.all 并发请求。
  // 下游的支付网关接口有严格的并发数限制（限流），并发会导致账户被临时锁定。请保持串行执行。
  ```

- **6) TODO 注释：** 标记尚未完成的工作。但请注意，团队必须定期使用全局搜索清理 TODO，否则它们最终会变成无人问津的垃圾。

- **7) 放大重要性 (Amplification)：** 某段看似微不足道、甚至有点像 bug 的代码，实际上极其重要，需要注释来强调。

### 3. 什么是“坏注释”？(The Bad Comments - 核心扫雷区)

书中用了极大的篇幅来痛批坏注释。在我们的日常 Code Review 中，特别是提交到 Gerrit 供同事审查时，以下这些注释是绝对不能出现的：

- **1) 喃喃自语 (Mumbling)：** 只有写的人自己懂，或者写得很随意，没给读者提供任何额外价值。

- **2) 多余的注释 (Redundant Comments)：** 读注释的时间比读代码的时间还长，而且完全是把代码翻译成中文。

  ```typescript
  // 如果 isVip 为 true，则执行后面的逻辑
  if (isVip) {
     // 计算折扣金额
     const discount = calculateDiscount();
  }
  ```

- **3) 误导性注释 (Misleading Comments)：** 曾经是对的，后来代码改了，注释没改。这种注释会把接手的同事坑惨，导致长达几小时的错误 Debug。

- **4) 循规蹈矩的注释 (Mandated Comments)：** 某些老派公司规定“每个函数、每个变量都必须有 JSDoc 注释”。这不仅徒增工作量，还会导致满屏幕废话。

  ```typescript
  /**
   * @param title 标题
   * @param content 内容
   * @returns 返回布尔值
   */
  function validatePost(title: string, content: string): boolean { ... }
  ```

- **5) 日志式注释 (Journal Comments)：** 

  ```typescript
   // 2023-01-05: 张三修复了空指针异常 
   // 2023-06-12: 李四增加了对移动端的兼容适配
  ```

  讲者强调：现代开发中，请完全信任我们的版本控制系统。谁、在什么时间、修改了什么，Git Log 和 Gerrit 的历史记录比任何手工注释都准确和详尽。不要在代码文件里写流水账！

- **6) 废弃的代码 (Commented-Out Code)：** 这是整洁代码的**头号公敌**！

  ```typescript
  // const oldApiUrl = 'https://api.v1.example.com';
  // fetch(oldApiUrl).then(...);
  ```

  看到被注释掉的代码，后面的开发者会有心理压力：“这代码是不是还有用？是不是等会儿还要放开？” 于是没人敢删，代码库越来越臃肿。 **终极法则：看到废弃代码，直接按 Delete 键！** 不要害怕丢失，Git 会永远记住所有的历史节点。

- **7) 位置标记 (Position Markers)：** 

   

  ```typescript
  // =================================== 
  // =========== API 请求方法 ============ 
  // ===================================
  ```

  偶尔用一次可以，但如果滥用，就会变成视觉噪音。如果你的文件需要这种横幅来区分区域，通常说明这个文件太大了，你应该把它拆分成多个小文件。

- **8) 非局部信息 (Non-local Information)：** 如果你一定要写注释，请确保注释只描述其附近的局部代码。不要在当前函数的注释里，去描述系统级别的其他配置规则。

------

## 第二部分：第五章 格式 (Formatting) —— 沟通是专业开发者的头等大事

很多人觉得代码格式是“个人审美”问题。Bob 大叔毫不客气地指出：**代码格式关乎沟通，而沟通是专业开发者的头等大事。** 你今天写的代码，也许几个月后就会被修改；如果你写的代码杂乱无章、没有规律可循，阅读者的耐心就会被消磨殆尽。代码的格式应当像报纸一样，清晰、有层次、主次分明。

### 1. 垂直格式 (Vertical Formatting)

垂直方向决定了我们阅读代码的连贯性。

- **1) 像读报纸一样 (The Newspaper Metaphor)：**

  - 最顶部：应该给出高层次概念和算法（模块说明、最核心的主入口函数）。
  - 向下展开：细节依次呈现。最底层的工具函数应该放在文件的最底部。这种自顶向下的阅读体验最符合人类思维。

- **2) 概念间距 (Vertical Openness Between Concepts)：**

  - 每一行展现一个表达式或一个子句，每一组代码行展示一个完整的思路。
  - **空白行就是视觉上的“标点符号”。** 不同的逻辑块之间、不同的函数之间、包引入（Imports）和业务逻辑之间，须用空行隔开。

  ```typescript
  // 糟糕：挤在一起，无法呼吸
  import { ref } from 'vue';
  import { fetchUser } from '@/api';
  const user = ref(null);
  const loadData = async () => { user.value = await fetchUser(); };
  loadData();
  
  // 整洁：用空行区分概念模块
  import { ref } from 'vue';
  import { fetchUser } from '@/api';
  
  const user = ref(null);
  
  const loadData = async () => { 
      user.value = await fetchUser(); 
  };
  
  loadData();
  ```

- **3) 垂直联系 (Vertical Density & Distance)：**

  - **紧密相关的代码应该相互靠近。**
  - **变量声明：** 尽可能靠近它们被使用的地方。对于简短的函数，局部变量应该在函数的顶部声明；如果在较长的函数中（虽然不推荐长函数），块级作用域变量（如 `for` 循环中的变量）应在块内声明。
  - **相关函数：** 如果函数 A 调用了函数 B，那么它们应该靠在一起，且调用者（A）应该尽可能放在被调用者（B）的上方。这就形成了一个自然的执行流向。

### 2. 水平格式 (Horizontal Formatting)

水平方向决定了阅读每一行时的视觉体验。

- **1) 行宽：** 保持行宽足够短。虽然现在的显示器越来越宽，但人类眼球左右扫视的舒适范围是有限的。建议遵循团队规范，通常不要超过 100 到 120 个字符。千万不要让阅读者频繁左右拖动滚动条。

- **2) 水平区隔与靠近 (Horizontal Openness and Density)：**

  - **赋值操作符（`=`, `==`, `===`）两边加上空格：** 这强调了左右两边的同等重要性，让等号更为醒目。
  - **函数参数之间的逗号后面加上空格：** 就像写英文句子一样。
  - **函数名与紧随其后的左括号之间不加空格：** 因为函数与其参数是紧密结合的统一体。

  ```typescript
  // 糟糕的间距
  function record(name,age){
      let total=name.length+age;
  }
  
  // 良好的间距
  function record(name, age) {
      let total = name.length + age;
  }
  ```

- **3) 拒绝水平对齐 (Horizontal Alignment)：** Bob 大叔专门批评了一种为了“好看”而刻意对齐一堆变量赋值的做法。

  TypeScript

  ```typescript
  // 糟糕的做法：刻意对齐
  let userName     = "admin";
  let userPassword = "password";
  let age          = 25;
  ```

  这看似整齐，实际上有害。首先，它把阅读者的视线从“变量的意义”吸引到了“等号的位置”上。其次，当你在下方加入一个名字很长的新变量时，你不得不重新排版上面所有的等号，这在版本控制工具（Git）中会产生大量毫无意义的 Diff 修改记录。*

### 3. 团队规则胜于一切 (Team Rules)

一个优秀的开发者，即使有自己偏好的代码风格，在参与团队项目时，也必须**绝对服从团队的格式规范**。

​	在现代开发环境中（尤其是像我们使用 TypeScript 和 Vue 的工程），我们不应该再依靠“肉眼”去在 Code Review 时检查格式（比如多了一个空格、少了一个分号）。我们应该全面引入 **Prettier** 和 **ESLint**。在每次执行 `git commit` 时，工具会自动将代码格式化为团队统一的标准。

------

## 第三部分：第六章 对象和数据结构 (Objects and Data Structures)

这一章触及了面向对象编程（OOP）和过程式编程（Procedural Programming）的最核心差异。这是很多哪怕写了多年代码的工程师都容易混淆的概念。很多人认为使用了 `class` 关键字，写了 `interface`，就是在进行面向对象编程，这其实是一个巨大的错觉。

### 1. 数据抽象 (Data Abstraction) 与本质区别

**核心定律：**

- **对象 (Objects)：** 隐藏内部的数据结构，对外暴露操作这些数据的行为（方法）。
- **数据结构 (Data Structures)：** 仅仅暴露数据本身，没有任何有意义的行为。

不要以为把类的变量声明为 `private`，然后给每个变量无脑加上 `get` 和 `set` 方法，它就变成了“对象”。如果外部依然可以随意获取和修改它的内部状态，那它本质上仍然是个数据结构。

**我们来看书中的一个对比：**

- **纯数据结构（暴露细节）：**

  ```typescript
  interface Point {
    x: number;
    y: number;
  }
  ```

  你一眼就能看透，它使用的是直角坐标系。

- **真正的对象（隐藏实现，暴露抽象概念）：**

  ```typescript
  interface Point {
    getCartesianX(): number;
    getCartesianY(): number;
    getR(): number;
    getTheta(): number;
  }
  ```

  在这个接口中，你根本不知道内部底层存储的是直角坐标（X, Y）还是极坐标（R, Theta）。它隐藏了实现，仅仅向外部暴露了行为和能力。这就是数据抽象。

### 2. 数据与对象的反对称性 (Anti-Symmetry)

这是本书中最精辟的论断之一。面向过程编程与面向对象编程没有绝对的优劣，它们有着高度对称互补的特点：

- **面向过程代码（使用单纯的数据结构）：**

  - **优势：** 很容易在不修改现有数据结构的前提下，添加**新的函数（新操作）**。
  - **劣势：** 很难添加**新的数据结构**，因为一旦添加，你就必须去修改所有与之相关的函数（比如要在所有的 `switch-case` 里加上新的条件）。

- **面向对象代码：**

  - **优势：** 很容易在不修改现有函数的前提下，添加**新的类（新类型）**（通过多态和继承）。

  - **劣势：** 很难添加**新的函数**，因为一旦要给基础接口增加一个新方法，你就必须去修改所有实现该接口的类。

    

   在复杂的系统设计中，如果你预期系统未来会不断增加**新的类型/业务实体**，你应该毫不犹豫地选择**面向对象编程**。 但如果你预期系统实体的种类很固定，但未来会不断添加对这些实体**新的操作/分析方式**，那么使用简单的数据结构配合**面向过程代码**（甚至函数式编程思想）反而更加优雅。一切取决于系统的演进方向。

### 3. 迪米特法则 (The Law of Demeter) - “不要和陌生人说话”

迪米特法则规定：**模块不应该了解它所操作对象的内部情形。** 具体的规则是，类 `C` 的方法 `f` 只能调用以下对象的方法：

1. `C` 自己
2. 由 `f` 创建的对象
3. 作为参数传递给 `f` 的对象
4. 由 `C` 的实例变量持有的对象

**典型反面教材：火车事故 (Train Wrecks)** 在代码中，我们经常能看到长长的一串链式调用：

```typescript
// 糟糕：严重的火车事故。调用者完全看透了底层的层级结构
const outputDir = context.getOptions().getScratchDir().getAbsolutePath();
```

这段代码严重违反了迪米特法则。调用者不仅知道 `context` 里面有 `options`，还知道 `options` 里面有 `scratchDir`。这就像是在直接翻找对象的内脏。一旦底层结构发生任何微小的改变，这段代码立刻崩溃。

**如何改进？让对象去做事，而不是向对象索要数据。** 调用者到底想要干什么？其实他只是想在这个绝对路径下创建一个临时文件。

```typescript
// 良好：告诉 context 你想要完成的意图，内部细节由它自己处理
const outputStream = context.createScratchFile("temp_file.txt");
```

看，我们隐藏了内部结构，让 `context` 对象暴露了“创建临时文件”的**行为**，而不是暴露它的**数据层级**。

### 4. 数据传送对象 (DTO - Data Transfer Object)

最典型、最纯粹的**数据结构**，就是 DTO。 DTO 是一个没有任何隐藏的私有变量、也没有任何业务处理函数的结构体。

​	我们的前端开发中，DTO 无处不在。当我们发起 Axios 请求时，传递给后端的 Payload，以及从后端接收到的 Response，我们在 TypeScript 中定义的那些 Interface，它们全都是 DTO。

```typescript
// 典型的 DTO
interface UserProfileResponse {
    readonly id: string;
    readonly username: string;
    readonly email: string;
    readonly avatarUrl: string | null;
}
```

**关键忠告：保持 DTO 的纯粹性！** 千万不要在 DTO 的定义中塞入业务逻辑函数（比如在上面的接口里写一个验证邮箱格式的方法，或者解析头像路径的方法），这会把它变成一半是对象、一半是数据结构的“混合体（Hybrids）”。这种四不像的设计，既缺乏针对新函数的保护，也缺乏针对新类型的保护，是系统设计的大忌。我们应该把逻辑剥离出来，放在专门的 Service 类或纯函数中处理。

------

## 🎤 结语与 Q&A (The Conclusion)

各位，今天我们深入探讨了 4 到 6 章的内容。最后总结一下这三条带回日常开发的准则：

1. **写注释之前先停下来想一想：** 我能不能通过重命名变量、提取一个小函数，让这段代码自己把话说清楚？
2. **遵守格式纪律：** 拥抱自动化工具，让团队提交的代码像出自同一个人之手。用空行来呼吸，像写文章一样排版代码。
3. **分清数据与对象：** 不要写一半数据一半对象的“混合体”。当你需要隐藏内部状态并暴露行为时，使用面向对象；当你纯粹需要传输和结构化数据时，老老实实写 DTO。

整洁的代码不是一蹴而就的，它存在于我们每天敲击键盘的每一个空格、每一次变量命名、以及每一次鼓起勇气删除旧代码的瞬间。

感谢大家的聆听！